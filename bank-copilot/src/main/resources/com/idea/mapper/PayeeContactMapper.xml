<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idea.mapper.PayeeContactMapper">

    <select id="selectPage" resultType="com.idea.entity.PayeeContact">
        select
            id,
            user_id,
            payee_name,
            account_no,
            alias,
            create_time,
            update_time
        from payee_contact
        where user_id = #{userId}
        <if test="query.payeeName != null and query.payeeName != ''">
            and payee_name like concat('%', #{query.payeeName}, '%')
        </if>
        <if test="query.accountNo != null and query.accountNo != ''">
            and account_no like concat('%', #{query.accountNo}, '%')
        </if>
        <if test="query.alias != null and query.alias != ''">
            and alias like concat('%', #{query.alias}, '%')
        </if>
        order by update_time desc, id desc
    </select>

</mapper>

